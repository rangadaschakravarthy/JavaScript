# Day 02 Mini-Project Expected Output

When inspecting the profile app interface:
- Initial render shows ID `USR-98421`, Name `Marcus Aurelius (Junior Developer)`, Age `24`.
- Clicking **Increment Age** updates age to `25` (demonstrates `let` reassignment).
- Clicking **Promote Role** updates role to `Senior Engineer` (demonstrates property mutation on a `const` object reference).
- Clicking **Test Const Reassign** triggers a console error and alert dialog: `TypeError: Assignment to constant variable`.
