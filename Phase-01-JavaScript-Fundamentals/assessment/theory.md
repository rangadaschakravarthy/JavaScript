# Phase 1 Assessment: Theory Questions (30 Questions)

1. Who created JavaScript, in which year, and at which company?
2. What is the difference between ECMAScript and JavaScript?
3. What role does TC39 play in language evolution?
4. What is JIT compilation in modern engines like V8?
5. Name three major JavaScript engines and the browsers/runtimes they power.
6. What is the difference between JavaScript core features and Web APIs?
7. Explain the difference between `<script async>` and `<script defer>`.
8. What does `"use strict"` do and why was it introduced in ES5?
9. What is Automatic Semicolon Insertion (ASI) and why can it cause bugs?
10. Differentiate between statements and expressions.
11. Explain the four stages of a variable's lifecycle.
12. Compare `var`, `let`, and `const` regarding scope, hoisting, and reassignability.
13. What is the Temporal Dead Zone (TDZ)?
14. Why does mutating a property of a `const` object not throw a `TypeError`?
15. What are the legal character rules for JavaScript identifiers?
16. Explain `camelCase`, `PascalCase`, and `UPPER_SNAKE_CASE` naming conventions.
17. Compare Global Scope, Function Scope, and Block Scope.
18. List all 7 Primitive Data Types in JavaScript.
19. What is the difference between `null` and `undefined`?
20. Why does `0.1 + 0.2 !== 0.3` in JavaScript?
21. What is `Number.MAX_SAFE_INTEGER` and why does `BigInt` exist?
22. What is `NaN` and why does `NaN === NaN` evaluate to `false`?
23. Why does `typeof null` return `"object"`?
24. How are primitives stored in memory versus reference objects (Stack vs Heap)?
25. Explain prefix increment (`++x`) versus postfix increment (`x++`).
26. Differentiate between Loose Equality (`==`) and Strict Equality (`===`).
27. How does short-circuit evaluation work in `&&` and `||`?
28. How does Nullish Coalescing (`??`) differ from Logical OR (`||`)?
29. What does Optional Chaining (`?.`) accomplish?
30. List all 8 falsy values in JavaScript.
