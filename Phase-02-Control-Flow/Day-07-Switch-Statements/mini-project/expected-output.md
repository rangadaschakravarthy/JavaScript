# Day 07 Mini-Project Expected Output

When selecting Option `1. Add (+)` with operands `50` and `10`:
- Menu Dispatch Result: `Option 1 (Add): 50 + 10 = 60`

When selecting Option `4. Divide (/)` with operands `50` and `0`:
- Menu Dispatch Result: `Option 4 (Divide): Error - Cannot divide by zero!`

When selecting Option `5. Exit System`:
- Menu Dispatch Result: `Option 5: System Exited. Goodbye!`
