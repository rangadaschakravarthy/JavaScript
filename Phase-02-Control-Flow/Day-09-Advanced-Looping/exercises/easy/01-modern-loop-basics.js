/**
 * Day 09 — Exercise 01 (Easy): Modern Loop Basics
 * 
 * INSTRUCTIONS:
 * Complete the function stubs below according to the specifications.
 * Do not modify function signatures or return types.
 */

/**
 * Task 1: Sum Array with for...of
 * Calculates the total sum of numbers in an array using a `for...of` loop.
 * @param {number[]} numbers - Array of numbers.
 * @returns {number} Sum of all numbers.
 */
function sumArrayWithForOf(numbers) {
  // TODO: Implement using a for...of loop
  return 0;
}

/**
 * Task 2: Count Key-Value Pairs
 * Counts the total number of own enumerable properties in an object using a `for...in` loop.
 * @param {Object} obj - Input object.
 * @returns {number} Total count of own properties.
 */
function countObjectKeysWithForIn(obj) {
  // TODO: Implement using a for...in loop with hasOwnProperty check
  return 0;
}

/**
 * Task 3: Reverse String with for...of
 * Reverses a string character by character using a `for...of` loop.
 * @param {string} str - Input text.
 * @returns {string} Reversed string.
 */
function reverseStringWithForOf(str) {
  // TODO: Implement using a for...of loop
  return '';
}

module.exports = {
  sumArrayWithForOf,
  countObjectKeysWithForIn,
  reverseStringWithForOf
};
