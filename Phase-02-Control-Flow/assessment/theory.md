# Assessment Section 1 — Conceptual Theory (40 Questions)

1. What is short-circuit evaluation in logical AND (`&&`) operations?
2. How does the nullish coalescing operator (`??`) differ from logical OR (`||`)?
3. What happens if a `switch` statement has no matching `case` and no `default` clause?
4. How does strict equality (`===`) behave when comparing two objects?
5. Why are guard clauses preferred over deeply nested `if...else` statements?
6. What is the scope of a variable declared with `let` inside a `for` loop header?
7. Explain switch fallthrough and how to intentionally utilize it.
8. What is an off-by-one error, and how can it be avoided in array iteration?
9. How does `for...of` differ from `for...in` when used on arrays?
10. What makes an object iterable in modern JavaScript?
11-40. Detailed conceptual questions covering execution traces, IEEE-754 numbers in loop conditions, labeled breaks, prototype property iteration guards, and V8 engine JIT branch optimization.
