/**
 * Day 12 — Exercise 01 (Easy): Nested Function Calls Basics
 */

function double(n) {
  return n * 2;
}

function addTen(n) {
  return n + 10;
}

/**
 * Task 1: Quadruple using double()
 * Implement quadruple(n) by calling double() twice.
 * @param {number} n 
 * @returns {number}
 */
function quadruple(n) {
  // TODO: Implement using double()
  return 0;
}

/**
 * Task 2: Double then Add Ten
 * Implement doubleThenAddTen(n) using double() and addTen().
 * @param {number} n 
 * @returns {number}
 */
function doubleThenAddTen(n) {
  // TODO: Implement
  return 0;
}

module.exports = {
  double,
  addTen,
  quadruple,
  doubleThenAddTen
};
