/**
 * Day 13 — Exercise 02 (Medium): Nested Scope Resolution
 */

const baseMultiplier = 2;

/**
 * Task: Compute Nested Power Scale
 * Outer function receives initial value `val`.
 * Inner function `scale(factor)` multiplies `val` by `factor` and `baseMultiplier`.
 * Returns result of calling `scale(3)`.
 * 
 * @param {number} val 
 * @returns {number}
 */
function createNestedScale(val) {
  // TODO: Implement inner scale function and return scale(3)
  return 0;
}

module.exports = { createNestedScale };
