# Interview Questions

### Q1: Why does typeof [] return "object"? How do you check if a variable is an array?
**Answer:** In JavaScript, Arrays are special object instances. Use `Array.isArray(val)` to correctly check if a variable is an array.
