// Student Ranking System CLI Entry Point
console.log("=== Student Ranking System CLI ===");
function runProject() {
  const sampleData = [10, 20, 30, 40, 50];
  const sum = sampleData.reduce((a, b) => a + b, 0);
  console.log("Sample Data Sum:", sum);
}
runProject();
